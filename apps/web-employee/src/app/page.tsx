import { FirstAccessPanel } from "./first-access-panel";

export default function EmployeeHomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-slate-50 px-5 py-8 text-slate-900">
      <FirstAccessPanel />
    </main>
  );
}
